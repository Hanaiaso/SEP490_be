> **Cập nhật 03/08/2026 (đợt 2):** Phase 1, Phase 2 (9/9 trừ 2 case cố tình hoãn) và Phase 3 (11/11) đã
> xử lý xong. Chi tiết ở cuối file, mục "Nhật ký xử lý".

# Kế hoạch xử lý lỗi còn tồn đọng — sau rà soát 03/08/2026

Nguồn đối chiếu:
- `Report_5_2_L1-UnitTests_VietTien_v2_3.xlsx` (cột **New Status** vừa được thêm — xem sheet `TestCase List` và từng sheet service)
- `VietTien.Tests/DOC_MISMATCHES.md` (đối chiếu doc ↔ code ngày 01/08/2026)
- Kết quả chạy thật `dotnet test` ngày 03/08/2026 sau commit `37ac5fb "fix bugs"` (nhánh `huy`, đã merge vào `main`): **392 Pass / 8 Fail / 12 Skip** (skip có từ trước, không liên quan)

## Tóm tắt

Commit `37ac5fb` (03/08/2026) đã sửa **21/54** case Fail/Blocked trong doc v2.3. Còn lại:

| Nhóm | Số case | Ý nghĩa |
|---|---|---|
| 🔴 Còn lỗi thật (có test đỏ, đã xác nhận bằng `dotnet test`) | 7 | Ưu tiên cao nhất — Phase 1 |
| 🟠 Spec gap — tính năng chưa cài đặt | 9 | Phase 2 |
| ⚪ Blocked kiến trúc — cần viết test L3, không phải lỗi code | 11 | Phase 3 |
| ⚫ Không xác minh được (code FE không nằm trong repo BE này) | 6 | Phase 4 — làm ở repo frontend |

Toàn bộ chi tiết case-by-case nằm trong cột **New Status** của file xlsx.

---

## Phase 1 — 🔴 Lỗi thật, ảnh hưởng tiền/bảo mật (làm trước, có test đỏ sẵn để verify)

### 1.1 `AuthService.ResendEmailOtpAsync` — 5 case: `L1-AUTH-26/28/29/30/31`
File: [AuthService.cs:152-180](VietTien.API/Services/Implementations/AuthService.cs:152)

Vấn đề (đã có test `[Fact]`/`[Theory]` đỏ sẵn trong `AuthServiceTests.cs`, chỉ cần code pass):
1. **User enumeration**: trả thẳng "Không tìm thấy tài khoản với email này." — lộ email nào đã đăng ký. `ForgotPasswordAsync` ở cùng service đã làm đúng (luôn trả cùng 1 câu) — copy pattern đó sang.
2. **OTP lưu thô**: `user.OtpCode = otpCode;` không băm. Copy cách làm của `RequestPhoneVerificationAsync` (đã sửa, dùng `BCrypt.Net.BCrypt.HashPassword`).
3. **Không throttle 60s / rate limit 5 lần-30 phút / 10 lần-ngày**: `RequestPhoneVerificationAsync` đã có pattern throttle+rate-limit theo cửa sổ 30 phút (dùng field `PhoneOtpWindowStart`/`PhoneOtpSendCount`) — cần thêm field tương tự cho email (`EmailOtpWindowStart`, `EmailOtpSendCount`) + **thêm bộ đếm theo ngày** (spec yêu cầu cả 30 phút VÀ ngày, mà bản fix OTP điện thoại cũng đang thiếu bộ đếm ngày — nên làm chung 1 lần cho cả 2 luồng).
4. (Đã ghi nhận ở doc, chưa có test đỏ) `catch` khi gửi mail lỗi vẫn trả `Success=true` — cần nhóm chốt hướng xử lý (trả lỗi thật hay giữ nguyên) trước khi sửa.

Gợi ý field cần thêm vào `User.cs` (giống pattern Phone đã có, cộng thêm bộ đếm ngày dùng chung cho cả Email/Phone nếu muốn tổng quát hoá):
```
public int EmailOtpSendCount { get; set; }
public DateTime? EmailOtpWindowStart { get; set; }
public int EmailOtpSendCountDaily { get; set; }
public DateTime? EmailOtpDayWindowStart { get; set; }
```
→ cần 1 migration mới.

### 1.2 `OrderService.ProcessSePayWebhookAsync` — 2 case: `L1-ORD-74/75` (+ `L1-ORD-77b` chưa có trong doc v2.3, xem mục "Ghi chú thêm")
File: [OrderService.cs:294-338](VietTien.API/Services/Implementations/OrderService.cs:294), lỗi cụ thể ở **dòng 317**:
```csharp
if (payload.transferAmount < order.FinalPayment) return;
```
- **`L1-ORD-74` (trả THIẾU)**: return im lặng — không log, không cảnh báo. Bên cạnh đó (dòng 335) nhánh "trả tiền sau khi đơn đã huỷ" **đã có** pattern đúng: gọi `CreateOrUpdatePaymentExceptionAsync(order, "PAID_AFTER_CANCELLATION", ...)`. Sửa bằng cách áp dụng đúng pattern này cho nhánh trả thiếu, ví dụ mã lỗi mới `"UNDERPAYMENT"`, giữ `PaymentStatus = Pending`, tạo cảnh báo cho Sales Manager.
- **`L1-ORD-75` (trả THỪA)**: điều kiện dùng `<` nên trả thừa vẫn lọt qua và set `Paid`. SRS BV-01 yêu cầu khớp **chính xác**. Đổi điều kiện thành khớp đúng (`!=`) và log đối soát tương tự (`"OVERPAYMENT"`) thay vì set Paid ngay.

---

## Phase 2 — 🟠 Spec gap: tính năng chưa cài đặt (9 case, cần thiết kế + code mới)

| Case | File / method | Việc cần làm |
|---|---|---|
| `L1-ORD-04` | [OrderService.cs:92 GetCheckoutSummaryAsync](VietTien.API/Services/Implementations/OrderService.cs:92) | Đơn ≥100M có `CustomerProfile.NegotiatedPrice` đã duyệt → tự áp giá, bỏ qua yêu cầu báo giá. Hiện hàm này chưa hề đọc `NegotiatedPrice`. |
| `L1-ORD-09` | `PlaceOrderAsync` | Chặn checkout khi `Cart.PriceLockedAt` quá 24h (hiện `CartService` chỉ tự refresh giá, không chặn). |
| `L1-ORD-24` | [OrderService.cs:516 PlaceDirectOrderAsync](VietTien.API/Services/Implementations/OrderService.cs:516) | Validate định dạng SĐT (regex 10 số) trước khi dùng, hiện chỉ check rỗng. |
| `L1-ORD-34` | [OrderService.cs:1626 RejectOrderAsync](VietTien.API/Services/Implementations/OrderService.cs:1626) | Bắt buộc `reason` không rỗng; hiện tham số `reason` nhận vào nhưng không dùng/không validate/không lưu. |
| `L1-ORD-43` | [OrderService.cs:1965 RecordDeliveryResultAsync](VietTien.API/Services/Implementations/OrderService.cs:1965) | Chặn thu COD (`dto.AmountCollected`) khi `order.PaymentStatus == Paid` (đã trả qua SePay) — hiện ghi đè `order.AmountPaid` vô điều kiện ở dòng ~2030. |
| `L1-ORD-44` | cùng method trên | Khi `outcome` là thất bại/từ chối, bắt buộc `reasonCode`; hiện DTO chỉ có `delivered/partially_delivered/failed`, không có field reason bắt buộc. |
| `L1-QUO-12` | `QuotationService.CustomerDecisionAsync` | Giới hạn 5 vòng đàm phán, vòng 6 tự động `Cancelled` + escalation. |
| `L1-VEH-05` | `VehicleService.UpdateAsync` | Chặn/chuyển-giao khi đổi xe đang có lịch giao sang `Maintenance` — cần liên kết `Vehicle` với bảng lịch giao (`Order.DeliveryVehicleId`/lịch chuyến), hiện `VehicleService` hoàn toàn không biết tới đơn hàng. |
| `L1-JOB-03` | `JobRunService.RunTrackedAsync` | Thêm cơ chế retry (hiện chỉ bọc đúng 1 lần chạy) với giới hạn số lần thử + cảnh báo khi vượt ngưỡng. |

**Ghi chú thêm — `L1-ORD-77b` (chưa có ID trong doc v2.3 nhưng đã có test đỏ)**: `vatAmount`/`discountAmount` trong [OrderService.cs:108-109](VietTien.API/Services/Implementations/OrderService.cs:108) không làm tròn về đồng nguyên (VND không có đơn vị < 1đ). Sửa bằng `Math.Round(..., MidpointRounding.AwayFromZero)` (hoặc quy ước làm tròn team thống nhất) cho cả `discountAmount` và `vatAmount`. Đề xuất bổ sung case này vào doc v2.4 với ID chính thức.

---

## Phase 3 — ⚪ Blocked do giới hạn kiến trúc L1 (11 case — KHÔNG PHẢI lỗi code, là nợ kiểm thử)

Các case này bị chặn phân quyền/định danh caller ở **Controller** (`[Authorize(Roles=...)]`) hoặc thiếu tham số caller trong signature service — không thể unit-test ở tầng service. Không cần sửa code nghiệp vụ, chỉ cần bổ sung **test tích hợp (L2/L3)** gọi qua `WebApplicationFactory`/`HttpClient` để test thật sự đi qua middleware `[Authorize]`:

`L1-AUTH-10`, `L1-AUTH-11` (Google OAuth static call), `L1-ORD-47`, `L1-ORD-53`, `L1-ORD-71`, `L1-QUO-09`, `L1-AUD-07`, `L1-MKT-03`, `L1-DASH-02`, `L1-DASH-05`, `L1-REG-03`.

→ Đề xuất: mở 1 dự án `VietTien.IntegrationTests` (L3) riêng, dùng `WebApplicationFactory<Program>` + SQLite in-memory, viết lại đúng 11 case này gọi qua Controller thật.

---

## Phase 4 — ⚫ Frontend (6 case, không xác minh được từ repo BE)

`L1-FES-02`, `L1-FEC-08`, `L1-FCMP-04`, `L1-FCMP-05`, `L1-FCMP-06`, `L1-FCMP-07`.

Repo `SEP490_be` hiện tại không chứa code frontend (không có `package.json`). Theo `DOC_MISMATCHES.md` (dòng 13), frontend nằm ở `D:\SEP490\SEP490_fe\frontend` — cần chạy `npx vitest run` ở repo đó để xác minh trạng thái thật, rồi áp dụng cùng cách đối chiếu (diff `fetchWithToken`, `CartContext.mergeGuestCartIntoServer`, `Checkout.jsx`, `PhoneVerificationModal`) đã ghi trong `DOC_MISMATCHES.md` mục 2.2/2.3.

---

## Thứ tự đề xuất triển khai

1. **Phase 1** (1.1 + 1.2) — có test đỏ sẵn, sửa xong chạy `dotnet test` là biết ngay đúng/sai. Ưu tiên cao nhất vì liên quan tiền thật (`ORD-74/75/77b`) và bảo mật (`AUTH-26/28/29/30/31`).
2. **Phase 2** — cần thiết kế thêm (đặc biệt `ORD-04` giá thoả thuận, `ORD-43/44` luồng giao hàng, `VEH-05` liên kết lịch chuyến) — nên họp chốt spec trước khi code vì một số case đụng vào luồng nghiệp vụ đang chạy.
3. **Phase 3** — làm song song, không phụ thuộc Phase 1/2, chỉ cần hạ tầng test L3.
4. **Phase 4** — chuyển cho người phụ trách FE hoặc chạy lại ở repo `SEP490_fe`.

Sau mỗi phase, chạy lại `dotnet test VietTien.Tests\VietTien.Tests.csproj --nologo -v q` và cập nhật cột **New Status** / **Status** trong `Report_5_2_L1-UnitTests_VietTien_v2_3.xlsx`.

---

## Nhật ký xử lý (03/08/2026, đợt 2)

Kết quả cuối: **`dotnet test VietTien.sln`** → 401 Pass (unit) + 17 Pass (L3 mới) / **0 Fail** / 2 Skip (cố tình hoãn).

### Phase 1 — xong 3/3
Đã sửa đúng như kế hoạch: `ResendEmailOtpAsync` (chống enumeration + băm OTP + throttle/rate-limit),
`ProcessSePayWebhookAsync` (log `PaymentException` UNDERPAYMENT/OVERPAYMENT thay vì return im lặng /
set Paid sai), làm tròn VAT/chiết khấu về đồng nguyên VND.

### Phase 2 — xong 7/9, hoãn 2/9
Đã sửa: `ORD-24` (validate SĐT), `ORD-34` (bắt buộc lý do từ chối), `ORD-43`/`ORD-44` (chặn COD trên
đơn đã Paid + bắt buộc reason code khi khách từ chối nhận hàng — thêm field DTO/Model mới), `QUO-12`
(giới hạn 5 vòng đàm phán), `VEH-05` (chặn bảo trì xe đang có chuyến).

**Vẫn hoãn có chủ đích** (giữ nguyên lý do đã nêu, tránh đoán sai): `ORD-04` (thiếu schema
`SavedB2BPriceSnapshot`), `ORD-09` (xung đột với hành vi auto-refresh giá cố ý của `CartService`,
test `L1-CART-03` đang xanh xác nhận). `JOB-03` cũng được xác nhận là **cố ý không sửa** — comment
sẵn có trong `JobRunServiceTests.cs` ghi rõ retry là trách nhiệm riêng từng job, thêm retry generic sẽ
phá giả định idempotency của ~10 job khác.

### Phase 3 — xong 11/11 (khác kế hoạch ban đầu: 3 case fix được thẳng ở L1, không cần L3)

Rà lại kỹ hơn phát hiện 3 case **thực ra sửa được ở tầng L1** thay vì chỉ viết test L3:

- **`L1-REG-03`**: `StockTransferService.ReceiveAsync` đổi chữ ký thêm `staffId`, chặn IDOR theo
  `User.AssignedWarehouseId` — **cùng cơ chế đã có sẵn cho `L1-REG-01`**. Tiện tay phát hiện và sửa
  luôn 1 lỗi liên quan: `InventoryController.AdjustInventory` thiếu `catch (UnauthorizedAccessException)`
  nên exception IDOR của `REG-01` trả về 400 thay vì 403 — đã thêm catch còn thiếu cho cả 2 controller.
- **`L1-AUTH-10`/`L1-AUTH-11`**: tách `GoogleJsonWebSignature.ValidateAsync` (static call ra Google) ra
  interface `IGoogleTokenValidator` tiêm qua DI — mock được ở unit test, không còn cần L2/L3.

**8 case còn lại thực sự cần L3** (role-gate ở `[Authorize(Roles=...)]` tại Controller, đúng như đánh
giá ban đầu): `ORD-47`, `ORD-53`, `ORD-71` (chỉ phần role-gate — phần IDOR phạm vi sâu hơn đã sửa riêng
ở L1, xem dưới), `QUO-09`, `AUD-07`, `MKT-03`, `DASH-02`, `DASH-05`.

→ Tạo project mới **`VietTien.IntegrationTests`** (thêm vào `VietTien.sln`):
- `WebApplicationFactory<Program>` thật (JWT Bearer + middleware `[Authorize]` chạy thật), DbContext
  thay bằng **EF InMemory** (không dùng SQLite — provider SQLite của EFCore không dịch được
  `Sum()` trên cột `decimal`, nhiều service dashboard/KPI dùng `Sum(decimal)`).
- `IntegrationTestBase.CreateClientAsAsync(role)`: tạo + lưu 1 `User` với role chỉ định, mint JWT thật
  qua `IJwtService` lấy từ DI, gắn `Authorization: Bearer` vào `HttpClient`.
- `RoleGateTests.cs`: mỗi case có 2 test — role SAI → 403 Forbidden; role ĐÚNG + id không tồn tại →
  response khác 403 (chứng minh request đã lọt qua Authorization, chạm tới tầng service thật).
- Thêm `public partial class Program { }` vào cuối `VietTien.API/Program.cs` (bắt buộc để
  `WebApplicationFactory<Program>` truy cập được class sinh ra từ top-level statements).

**`L1-ORD-71` — cập nhật (04/08/2026): IDOR đã sửa.** Phần role-gate (Customer/Warehouse bị chặn hẳn)
vẫn được xác nhận ở L3 như trước. Phần IDOR sâu hơn — 1 Sales Staff xem được chi tiết đơn của khách do
Sales KHÁC phụ trách — đã sửa ở L1: `GetSalesOrderDetailAsync` nay nhận thêm tham số tuỳ chọn
`Guid? salesStaffId`, scope theo snapshot `order.SalesStaffId` (đúng cơ chế đã dùng sẵn ở
`GetSalesOrdersAsync`/`SYS_02`) — `SalesStaff` chỉ xem được đơn mình phụ trách, `SalesManager`/`Admin`
truyền `null` nên vẫn xem được mọi đơn. `OrderController.GetSalesOrderDetail` bắt riêng
`UnauthorizedAccessException` → `Forbid()` (403), tránh bị catch-all `Exception` nuốt mất thành 400.
Test: `VietTien.Tests/Services/OrderServiceTests.Scoping.cs` (`L1_ORD_71_GetSalesOrderDetail_*`, 3 case).

### File/thư mục mới tạo trong đợt này
- `VietTien.IntegrationTests/` (project L3 mới)
- `VietTien.API/Services/Interfaces/IGoogleTokenValidator.cs` + `.../Implementations/GoogleTokenValidator.cs`
- 2 migration: `AddEmailOtpRateLimitFields`, `AddOrderDeliveryRejectionReasonCode`
