# Kế hoạch xử lý lỗi — sau rà soát Report_5_2_L1 v3.1.1 & Report_5_2_L2 v2.5 (07/08/2026)

> **Cập nhật 07/08/2026 (đợt 3 — ĐÃ SỬA XONG TOÀN BỘ 16 case còn lại):**
> Sau đợt 2 (bên dưới), 16 case L2 còn Fail đã được xử lý hết — 10 bug ứng dụng thật (GH-01, GH-02,
> GH-06, GH-08, GH-09, GH-10, GH-12, GH-13, GH-14, GH-15, BR-019) và 6 lỗi nằm ở chính test tích hợp
> (PUR-04 sai enum + hiểu sai state machine, SJOB-10/11 thiếu FK bắt buộc trong seed, FLOW-02 chọn ca
> giao hết hạn + thiếu khai báo outbound Cloudinary, và phần còn lại của RET-01/02/03/04 từ đợt trước).
> Chi tiết từng fix nằm trong các mục Phase 0/1 bên dưới, đã gắn nhãn `[XÁC NHẬN RUNTIME]`.
>
> **Kết quả cuối cùng: `dotnet test VietTien.Tests` = 1262/1262 Pass, `dotnet test
> VietTien.IntegrationTests` = 110/110 Pass — toàn bộ xanh.** Cột Fix Status trong
> `Report_5_2_L2-IntegrationTests_VietTien_v2_5.xlsx` đã cập nhật theo kết quả này.
>
> Thay đổi code chính (tham khảo diff thật để biết chi tiết, tóm tắt ở đây):
> - `Order` model: thêm `RowVersion` (concurrency token, migration `AddOrderRowVersion`) — giải quyết cả
>   GH-02 (thanh toán kép) lẫn GH-15 (duyệt huỷ song song) cùng lúc, vì cả 2 đều là race condition trên
>   cùng 1 Order row.
> - `SePayController` + `OrderService.ProcessSePayWebhookAsync`: bỏ hẳn nhánh bypass theo
>   `ASPNETCORE_ENVIRONMENT` (GH-01).
> - `OrderController`/`DeliveryController`: bỏ các `catch (InvalidOperationException) return BadRequest`
>   cục bộ còn sót (GH-09, GH-14) — để `ExceptionHandlingMiddleware` map đúng 409; thêm catch riêng cho
>   `DbUpdateConcurrencyException` -> 409 ở 3 endpoint liên quan tới Order (webhook, manual-confirm,
>   approve-cancel-replacement).
> - `StockTransferService.DispatchAsync/ReceiveAsync` + `InventoryService.AdjustInventoryAsync`: ghi
>   `StockTransaction` cho mọi biến động tồn (GH-06, GH-12).
> - `OrderService.ConfirmPickupAsync`: cộng `OnHandQuantity` + `QuarantineQuantity` cho hàng thu hồi từ
>   khách (BR-019) — hàng đã về kho vật lý nhưng chưa khả dụng tới khi qua kiểm tra.
> - `OrderService.CalculateDiscountAsync`: tra cứu `Quotation` `CustomerAccepted` còn hiệu lực trước khi
>   chặn đơn ≥ngưỡng B2B (GH-13).
> - `OrderService.PlaceOrderAsync`: check hạn giữ giá 24h TRƯỚC khi gọi `CartService.GetCartAsync` (hàm
>   đó tự làm mới giá ngay khi đọc, nên check sau sẽ không bao giờ thấy hết hạn) (GH-08).
> - `PurchaseOrderService.ImportFromExcelAsync`: đọc thật nội dung file (CSV: `ProductSku,Quantity,
>   UnitPrice`) thay vì tạo PO rỗng (GH-10). *Giới hạn đã biết: chưa parse file `.xlsx` nhị phân thật —
>   xem TODO trong code.*
>
> **Cập nhật thêm cho L1** (`Report_5_2_L1-UnitTests_VietTien_v3_1_1.xlsx`, cột Fix Status + Status):
> - `L1-REG-01` (GH-12): **ĐÃ FIX hoàn toàn** — `AdjustInventoryAsync` nay ghi `StockTransaction`, xác nhận qua L2-TRF-04 PASS.
> - `L1-ORD-04` (GH-13) và `L1-ORD-09` (GH-08): **ĐÃ FIX hoàn toàn, kể cả tầng unit.** 2 test trong
>   `OrderServiceTests.cs` đã được viết lại với assertion thật (không còn body rỗng) và bỏ
>   `[Fact(Skip = "...")]`:
>   - `L1_ORD_04_Checkout_100M_WithAcceptedQuotation_NegotiatedPriceApplied` — seed `Quotation` +
>     `QuotationVersion` (`CustomerAccepted`, còn hiệu lực) rồi assert `GetCheckoutSummaryAsync` áp đúng
>     giá đã thoả thuận thay vì throw. Lưu ý cơ chế THẬT dùng bảng `Quotation`/`QuotationVersion.
>     ProposedTotal`, KHÔNG PHẢI field `CustomerProfile.SavedB2BPriceSnapshot` như test case gốc giả định.
>   - `L1_ORD_04b_Checkout_100M_WithExpiredAcceptedQuotation_StillBlocked` (case mới, biên hạn hiệu lực)
>   - `L1_ORD_09_PlaceOrder_PriceExpiredCart_Blocked` — seed `Cart.UpdatedAt` quá 24h rồi assert
>     `PlaceOrderAsync` throw, không tạo `Order`.
>   - `L1_ORD_09b_PlaceOrder_PriceNotYetExpiredCart_Allowed` (case mới, biên chưa hết hạn)
>
>   Cả 4 test PASS. Cột `Status` (không chỉ `Fix Status`) của 2 dòng này cũng đã đổi từ `Blocked` sang
>   `Pass`. `dotnet test VietTien.Tests`: 1266/1266 Pass, 0 Skip (trước đó 1264 total, 2 Skip).
> - Các mục L1 khác (OTP hardcode-vs-config `AUTH-20/28/29/30/31`, `L1-SJOB-11` off-by-one ngưỡng cảnh báo,
>   `L1-JOB-03` không có retry, `L1-FCMP-*`/`L1-FES-02`/`L1-FEC-08` phía frontend, và các case L3GAP) —
>   **KHÔNG đổi**, chưa được đụng tới, xem lại Phase 1/3 gốc bên dưới.
>
> **Phát hiện phụ đáng chú ý:** commit "seed data warehouse" (nhánh `dvdung`, merge qua PR #77 trong lúc
> đang làm đợt này) mắc **đúng lỗi drift giống hệt** đã vá ở đợt 2 — cập nhật `ApplicationDbContextModelSnapshot.cs`
> với seed data mới (2 kho vệ tinh, 4 user, sản phẩm, NVL, tồn kho) nhưng không kèm migration
> `InsertData` thật. Đã bổ sung migration `AddWarehouseSeedData` (xác minh diff rỗng bằng `dotnet ef
> migrations add`). Seed data mới này cũng làm lộ ra 2 test khác chọn "dòng Inventory đầu tiên" không
> pin theo kho cụ thể (`L2_FUL_07`, `SeedStockAsync` trong `L2WorkflowsTests.cs`) — đã sửa để luôn chốt
> đúng kho WH-DEFAULT bằng `WarehouseLocationId` cụ thể thay vì `FirstAsync` không có điều kiện kho.

> **Cập nhật 07/08/2026 (đợt 2 — xác nhận bằng Docker + `dotnet test VietTien.IntegrationTests` thật):**
> Sau khi có Docker, chạy thử suite integration (98 test, SQL Server thật qua Testcontainers) lần đầu ra
> **91 Fail / 19 Pass / 110 Total** — gần như mọi thứ đỏ. Đào vào message lỗi thật phát hiện nguyên nhân
> KHÔNG phải 91 lỗi nghiệp vụ riêng lẻ, mà là **1 lỗi migration chặn toàn bộ**: migration
> `20260806232730_FixSchemaDrift.cs` (thêm trong commit `bo sung test de coverage`) duplicate 100%
> những gì migration `20260806165447_InitData.cs` đã tạo sẵn 7 tiếng trước đó cùng ngày (bảng
> `MarketingPosts`/`ReturnExchangeRequests`+2 bảng con, cột `GoodsIssues`(8)/`GoodsReceipts.ImageProofUrl`/
> `Orders.ShippingAddress`/`StockTransactions.Note`/`StockTransfers`(3)) → SQL Server báo lỗi "column
> specified more than once" ngay khi `Migrate()` chạy trên DB rỗng — **lỗi này chặn cả việc khởi tạo
> database thật (fresh deploy/CI), không chỉ riêng test**.
>
> **Đã xử lý:** xoá `20260806232730_FixSchemaDrift.cs` + `.Designer.cs` (xác nhận từng dòng trùng 100%
> với `InitData` trước khi xoá). Build lại phát hiện thêm 1 drift THỨ HAI (độc lập, từ commit "add api to
> review product" cùng đợt pull): entity `ProductReview` + `Product.AverageRating`/`ReviewCount` đã có
> trong `ApplicationDbContextModelSnapshot.cs` nhưng **chưa từng có migration thật tạo bảng/cột** — đã bổ
> sung migration `20260807102804_AddProductReviews.cs` (hand-written, verify bằng `dotnet ef migrations add`
> ra diff rỗng = khớp 100% với model).
>
> **Sau khi vá cả 2 migration, chạy lại: 91 Pass / 19 Fail / 110 Total** (sau khi sửa tiếp 4 lỗi trong
> chính test `L2AfterSalesReturnTests.cs`: **94 Pass / 16 Fail / 110 Total**) — tăng từ 22 lên 94 Pass, xác
> nhận lỗi migration là nguyên nhân che lấp gần như toàn bộ. **19 case fail còn lại khớp gần như 1-1 với
> 18 case L2 đã theo dõi trong bảng dưới** (17 khớp trực tiếp + 1 test hạ tầng ngoài phạm vi 2 báo cáo) —
> tất cả đã được xác nhận lại bằng bằng chứng runtime thật (status code, message lỗi SQL thật), không còn
> dựa suy luận đọc code tĩnh nữa. Cột **Fix Status** trong `Report_5_2_L2-IntegrationTests_VietTien_v2_5.xlsx`
> đã được cập nhật theo kết quả thật này. Điểm khác biệt đáng chú ý so với đánh giá tĩnh trước đó:
> - **`L2-RET-06`/GH-14 nặng hơn đánh giá cũ**: route thật khách hàng gọi (`POST /api/delivery/{id}/request-cancel`
>   → `DeliveryController.cs:97` → `OrderService.RequestCancelPaidOrderAsync`, **KHÔNG PHẢI**
>   `RequestCancelOrderAsync` mà bản đọc code tĩnh trước đó đã xác nhận có guard) — hàm thật sự được gọi
>   hoàn toàn KHÔNG có check `DeliveryStatus`, trả 200 thành công thay vì bị chặn.
> - **`L2-RET-02/03/04` đã sửa xong ở tầng test, giờ PASS thật** — `L2AfterSalesReturnTests.cs` có tới
>   4 lỗi test-authoring độc lập chồng lên nhau: (1) `SeedStockAsync` lấy `CustomerProfile` đầu tiên
>   trong DB thay vì đúng profile của client `customer` vừa tạo (lộ ra sau khi `L1-ORD-51` chặn IDOR
>   đúng); (2) DTO duyệt yêu cầu là `IsApproved` (bool) nhưng test gửi `Action: "Approve"` (field không
>   tồn tại) → luôn bị hiểu ngầm là từ chối; (3) DTO tạo yêu cầu là `ReturnItems` (có validation
>   `[Range(1,max)]`) nhưng test gửi `Items` → danh sách hàng luôn rỗng, validate BV-03 không bao giờ
>   chạy tới; (4) DTO lên lịch điều xe cần `VehicleId`+`Shift`+`PickupDate` nhưng test gửi
>   `ScheduledDate` (field không tồn tại). Đã sửa cả 4 trong `VietTien.IntegrationTests/L2AfterSalesReturnTests.cs`
>   — `RET-02/03/04` giờ chạy PASS thật, xác nhận các business rule liên quan (phân quyền duyệt, BV-03 số
>   lượng, giá đơn thay thế cùng SKU) đều đã đúng, không cần sửa code ứng dụng.
> - **`L2-RET-01` sau khi sửa hết lỗi test lại lộ ra 1 bug thật MỚI**: `ConfirmPickupAsync`
>   (`OrderService.cs:2680-2691`) chỉ đổi `PickupStatus = PickedUp`, **không hề cộng `QuarantineQuantity`**
>   của tồn kho — vi phạm BR-019 (hàng thu hồi từ khách phải vào khu cách ly, không được cộng thẳng vào
>   khả dụng). Grep xác nhận `QuarantineQuantity` không được ghi ở bất kỳ đâu trong `OrderService.cs`.
> - **`L2-PUR-04`** thất bại vì test gửi `ResolutionType` không hợp lệ — có dấu hiệu business rule đã được
>   cài đặt đúng, chỉ là test tích hợp chưa cập nhật theo enum mới. Cần sửa test trước khi kết luận.
> - **`L2-SJOB-10`/`L2-SJOB-11`** thất bại vì lỗi FK trong chính test seed (`MarketingPosts.CreatedByUserId`
>   trỏ tới User chưa tồn tại) — lỗi test, không phải lỗi `JobRunService`/`MarketingPostService`.
> - **`L2-FLOW-02`** thất bại vì có vẻ đụng 1 business rule MỚI (chặn xác nhận giao hàng trước ngày hẹn)
>   mà kịch bản test COD chưa cập nhật ngày cho khớp — cần rà lại, chưa chắc là bug.
>
> Toàn bộ nội dung Phase 0-3 bên dưới **giữ nguyên phân tích gốc** (vẫn đúng và là nơi tra file:line), chỉ
> bổ sung nhãn `[XÁC NHẬN RUNTIME]` vào các mục đã có bằng chứng chạy thật ở đợt cập nhật này.

## Nguồn đối chiếu

- `Report_5_2_L1-UnitTests_VietTien_v3_1_1.xlsx` (391 test case) và `Report_5_2_L2-IntegrationTests_VietTien_v2_5.xlsx` (test tích hợp) — **đã thêm cột `Fix Status` mới** vào từng sheet service + cột `Fix Status (live link)` vào sheet `TestCase List` của cả 2 file.
- Đối chiếu bằng cách đọc trực tiếp code hiện tại (5 agent điều tra song song theo từng cụm service) cho toàn bộ **72 case Fail/Blocked** (54 case L1 + 18 case L2, một số defect trùng nhau giữa 2 file).
- Xác nhận thêm bằng chạy thật `dotnet test VietTien.Tests` (không cần Docker): **408 Pass / 1 Fail / 2 Skip / 411 Total** — xác nhận phần lớn phân loại "FIXED" ở tầng unit là chính xác.
- **Chưa chạy được `VietTien.IntegrationTests`** (cần Docker + SQL Server container, môi trường hiện tại không có Docker) — các phân loại liên quan L2 (đặc biệt GH-01/02/09/13/14/15) dựa trên đọc code, **nên chạy lại bằng Docker để có bằng chứng runtime thật** trước khi đóng case.

## Tóm tắt phân loại (72 case)

| Trạng thái | L1 | L2 | Ý nghĩa |
|---|---|---|---|
| ✅ ĐÃ FIX | 32 | 3 | L1: đúng như kỳ vọng test, có bằng chứng code. L2: `L2-RET-02/03/04` — đã sửa 4 lỗi test-authoring trong `L2AfterSalesReturnTests.cs`, giờ PASS thật, xác nhận business rule liên quan đều đúng |
| ⚠ FIX MỘT PHẦN | 8 | 0 | Đã sửa 1 khía cạnh, còn thiếu phần khác |
| ❌ CHƯA FIX (bug ứng dụng thật) | 8 | 11 | Vẫn còn nguyên vấn đề — L2 đã **xác nhận bằng chạy `dotnet test` thật qua Docker**. Bao gồm `L2-RET-01` — phát hiện MỚI sau khi sửa test: `ConfirmPickupAsync` không cộng `QuarantineQuantity` (BR-019), xem mục 1.3a |
| ❔ LỖI Ở TEST, KHÔNG PHẢI BUG | 0 | 3 | `dotnet test` thật đỏ nhưng do chính test tích hợp seed/gửi sai dữ liệu (`L2-PUR-04`, `L2-SJOB-10/11`) — cần sửa test trước khi kết luận lại |
| ❔ CẦN RÀ LẠI (chưa rõ bug hay do test) | 0 | 1 | `L2-FLOW-02` — có thể đụng business rule mới, kịch bản test chưa cập nhật |
| 🔒 CẦN L3 (không phải bug) | 6 | 0 | Đã có test L3 thật xác nhận (`VietTien.IntegrationTests/RoleGateTests.cs`) — không cần sửa code |

**Bonus finding ngoài phạm vi 2 báo cáo**: (1) đã phát hiện và fix 1 lỗi migration nghiêm trọng chặn toàn bộ suite integration (xem box cập nhật đầu file) — ưu tiên cao nhất vì chặn cả deploy/CI thật, không chỉ test; (2) đã sửa 4 lỗi test-authoring trong `L2AfterSalesReturnTests.cs`, qua đó phát hiện 1 bug thật mới (`L2-RET-01`/BR-019, mục 1.3a). Kết quả `dotnet test VietTien.IntegrationTests` cuối cùng: **94 Pass / 16 Fail / 110 Total**.

---

## Phase 0 — 🔴 P0: Tiền/bảo mật, đang production-risk (làm trước tiên)

### 0.1 GH-02 — Thanh toán kép (`L2-PAY-09`) `[XÁC NHẬN RUNTIME]`
File: `OrderService.cs:311-460` (`ProcessSePayWebhookAsync`), `ManualPaymentService.cs:27-143` (`ManualConfirmAsync`)

Chạy thật (Docker + SQL Server, 07/08/2026): 2 request song song vẫn tạo 2 `PaymentTransaction` cho cùng 1 đơn (mã `FTa0b5f52a5da9` + `MC7a00aa9dd9f3`).

`Order` không có `RowVersion`/concurrency token (6 bảng khác đã có từ commit `6f7e4c8` nhưng `Order` bị bỏ sót). `PaymentTransactions.OrderId` không có unique index. Cả 2 luồng thanh toán đọc `PaymentStatus=Pending` không khoá trước khi ghi → 2 request song song (webhook SePay + xác nhận thủ công) có thể cùng tạo giao dịch.

**Việc cần làm:**
1. Thêm `RowVersion` (concurrency token) vào `Order` — 1 migration mới.
2. Trong `ProcessSePayWebhookAsync` và `ManualConfirmAsync`, bọc đọc+ghi trong 1 transaction, dùng `ExecuteUpdate`/kiểm tra `RowVersion` khi lưu, bắt `DbUpdateConcurrencyException` → trả lỗi có ý nghĩa (đối phương đã xử lý trước) thay vì để lọt qua.
3. Cân nhắc thêm unique filtered index trên `PaymentTransactions` cho `(OrderId)` khi `Status = Success`, để chặn ở tầng DB luôn (defense in depth).

### 0.2 GH-15 — Nhân đôi giá trị đã thanh toán (`L2-RET-07`) — **nặng nhất dự án theo báo cáo gốc**
File: `OrderService.cs:2739-2860` (`ApproveCancelAndCreateReplacementAsync`)

Có `BeginTransactionAsync()` và check `OrderStatus != CancelRequested`, nhưng đọc bằng `FirstOrDefaultAsync` thường (không khoá hàng), và `Order` không có concurrency token (xem 0.1) → 2 request duyệt huỷ song song vẫn có thể cùng đi qua check, cùng tạo đơn thay thế + `PaymentReallocation`.

**Việc cần làm:** cùng cơ chế với 0.1 (RowVersion trên Order sẽ tự động giúp cả 2 case này — nên làm chung 1 migration). Sau khi có RowVersion, sửa `ApproveCancelAndCreateReplacementAsync` để lưu qua `SaveChangesAsync` có kiểm tra concurrency, bắt exception và trả 409 nếu đơn đã bị request khác xử lý trước.

### 0.3 GH-01 — Bypass xác thực webhook SePay (`L2-PAY-10`) `[XÁC NHẬN RUNTIME]`
File: `SePayController.cs:44-54`, `OrderService.cs:303-324`

Chạy thật: thiếu token + `ASPNETCORE_ENVIRONMENT=Development` → vẫn trả **200** (kỳ vọng 401).

3 vấn đề cùng khu vực, vẫn còn nguyên:
1. Khi thiếu token và `ASPNETCORE_ENVIRONMENT=Development` (biến môi trường tiến trình, không phải `IHostEnvironment`), code chỉ **log warning rồi cho qua** thay vì trả 401 — dòng `OrderService.cs:317-320`.
2. Token vẫn nhận qua **query string** (`SePayController.cs:44-48`) — lọt vào access log/proxy log (NFR-SEC06).
3. Vẫn dùng **1 secret tĩnh dùng chung** (`_configuration["SePaySettings:ApiToken"]`), không phải chữ ký HMAC per-request → không chống được replay.

**Việc cần làm:**
1. Đổi `Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT")` sang inject `IHostEnvironment` và chỉ cho phép bypass khi **thực sự** đang chạy test (`IsEnvironment("Test")`), không dựa vào biến môi trường tiến trình có thể bị set sai trên server thật.
2. Bỏ code bypass "log warning rồi cho qua" — luôn `throw UnauthorizedAccessException` khi token sai/thiếu, map 401.
3. Chuyển sang nhận token qua header (`X-SePay-Signature` hoặc tương tự), không dùng query string.
4. (Có thể làm sau) đổi sang chữ ký HMAC per-request nếu SePay hỗ trợ, thay vì static token.

### 0.4 GH-13 — Chuỗi B2B ≥100M không đặt được đơn (`L1-ORD-04`, `L2-FLOW-06`) — chặn hẳn 1 tính năng "đinh" `[XÁC NHẬN RUNTIME]`
File: `OrderService.cs:77-93` (`CalculateDiscountAsync`, dùng chung bởi `GetCheckoutSummaryAsync` và `PlaceOrderAsync`)

Chạy thật: sau khi có `Quotation.Status = CustomerAccepted` còn hiệu lực, checkout vẫn trả 400 "Đơn hàng trên 100,000,000đ vui lòng liên hệ NV Bán hàng".

Hàm chặn cứng **mọi** đơn ≥ `QUOTATION_MIN_VALUE` (mặc định 100tr) bằng thông báo "vui lòng liên hệ NV Bán hàng", **không hề tra cứu** xem khách đã có `Quotation` ở trạng thái `CustomerAccepted` còn hiệu lực hay chưa. Vì `L1-QUO-12` (giới hạn 5 vòng đàm phán) đã được sửa xong, luồng báo giá B2B giờ có thể đi tới `CustomerAccepted` được — nhưng **không có đường nào để đặt đơn** sau đó. Đây là bug chặn doanh thu thật, không phải edge case.

**Việc cần làm:**
1. Trong `CalculateDiscountAsync` (hoặc trước khi gọi), tra `Quotation` mới nhất của khách có `Status == CustomerAccepted`, `AcceptedVersionId` đã set, `ValidUntil >= now`.
2. Nếu có: dùng giá đã duyệt trong quotation, bỏ qua chặn ≥100M (đây chính là `L1-ORD-04`).
3. Nếu không có: giữ nguyên hành vi chặn hiện tại.
4. Chú ý: báo cáo gốc nghi ngờ có **2 endpoint tính tổng giỏ hàng từ 2 nguồn khác nhau** (GH-11) — cần rà cả `CartService` lẫn `OrderService` để đảm bảo áp giá nhất quán ở cả bước xem tổng và bước đặt hàng thật.

---

## Phase 1 — 🟠 P1: Lỗi thật, ảnh hưởng vận hành/kiểm toán

### 1.1 Controller nuốt `InvalidOperationException` thành 400 thay vì để middleware map 409 — **lỗi hệ thống, lặp lại ít nhất 2 nơi** `[XÁC NHẬN RUNTIME]`
- `GH-09` (`L2-FUL-06`): `OrderController.cs:376-379` (`ConfirmSalesOrder`) tự `catch (InvalidOperationException) { return BadRequest(...) }`. **Chạy thật xác nhận**: xác nhận đơn Cancelled trả 400 "Chỉ đơn hàng ở trạng thái 'Mới' hoặc 'Chờ xác nhận'..." thay vì 409.
- `GH-14` (`L2-RET-06`): **CẬP NHẬT sau khi chạy thật — nặng hơn đánh giá ban đầu.** Route khách hàng thật sự gọi là `POST /api/delivery/{orderId}/request-cancel` → `DeliveryController.cs:97` (`RequestCancelPaidOrder`) → `OrderService.RequestCancelPaidOrderAsync` (`OrderService.cs:2698-2733`) — **KHÔNG PHẢI** `OrderController`/`RequestCancelOrderAsync` như đánh giá tĩnh ban đầu (hàm đó có route `request-cancel` riêng ở `OrderController.cs:410`, ít nhất 2 route trùng tên khác controller, dễ nhầm). `RequestCancelPaidOrderAsync` chỉ check `PaymentStatus == Paid` và trạng thái yêu cầu huỷ trước đó (2703-2707) — **hoàn toàn không check `DeliveryStatus`** — nên đơn `InDelivery` vẫn được nhận yêu cầu huỷ, trả 200 kèm "Yêu cầu hủy đơn đã được gửi lên Sales Manager để phê duyệt." Đây không phải lỗi HTTP code như tưởng — đây là **thiếu hẳn business guard**, y hệt `RequestCancelOrderAsync` (đã có guard đúng, xem `L1-REG-06`) nhưng route/hàm khác lại thiếu.

**Việc cần làm:**
1. Thêm guard `DeliveryStatus` (chặn khi `InDelivery`, giống pattern đã có ở `RequestCancelOrderAsync`) vào `RequestCancelPaidOrderAsync` (`OrderService.cs:2698`).
2. Rà toàn bộ `OrderController.cs`/`DeliveryController.cs` (và các controller khác) tìm pattern `catch (InvalidOperationException ...) return BadRequest(...)` — xoá các catch cục bộ này, để exception đi qua `ExceptionHandlingMiddleware.cs` (đã map đúng 409 sẵn). Đây là sweep 1 lần — tìm bằng `grep -rn "catch (InvalidOperationException" VietTien.API/Controllers`.

### 1.2 Thiếu ghi sổ cái tồn kho (`StockTransaction`) — vi phạm BR-035/BR-044/BR-022 `[XÁC NHẬN RUNTIME]`
- `GH-06` (`L2-TRF-01`): `StockTransferService.cs` — `DispatchAsync`/`ReceiveAsync` đổi `OnHandQuantity`/`InTransitQuantity` trực tiếp, **0 tham chiếu** `StockTransaction` trong cả file. Chạy thật: đếm `StockTransactions` sau dispatch+receive = 0.
- `GH-12` / `L1-REG-01` (`L2-TRF-04`): `InventoryService.cs:126-127` — IDOR đã fix, nhưng `AdjustInventoryAsync` vẫn còn nguyên comment thừa nhận không ghi audit ("update inventory directly per requirements"). Chạy thật: không có `StockTransaction` nào được tạo sau `AdjustInventoryAsync`.

**Việc cần làm:** cả 2 service đều cần thêm bước tạo `StockTransaction` (delta, staff id, note, reference) mỗi khi thay đổi số lượng tồn — dùng đúng pattern đã có sẵn ở `GoodsIssueService` (đã ghi đúng theo báo cáo gốc). Copy pattern đó sang 2 service này.

### 1.3 GH-10 — Import Excel/OCR đơn mua hàng là stub nguy hiểm (`L2-PUR-05`) `[XÁC NHẬN RUNTIME]`
File: `PurchaseOrderService.cs:99-138`

Chạy thật: import file Excel có 3 dòng nhưng PO tạo ra có 0 dòng.

`ImportFromExcelAsync`/`ImportFromImageAsync` còn nguyên `// TODO: Use EPPlus or ClosedXML...` — **không parse file**, tạo PO rỗng với supplier/warehouse lấy đại (`.FirstOrDefault()`), chỉ lưu tên file vào `Note`, vẫn trả **200 OK** khiến người dùng tưởng đã nhập thành công. Đây là "thất bại không ồn ào" — nguy hiểm hơn cả việc chưa làm.

**Việc cần làm:**
1. Trước mắt (ngắn hạn, để không đánh lừa người dùng): đổi endpoint trả lỗi rõ ràng ("Tính năng import Excel/OCR chưa khả dụng") thay vì tạo PO rỗng giả.
2. Về lâu dài: cài `ClosedXML`/`EPPlus` để parse Excel thật theo đúng scope mới của SRS v2 (chỉ tạo **Draft + preview**, lỗi trả `ImportRowError` theo từng dòng) — việc này cần thiết kế riêng, không làm vội trong sweep P1.

### 1.3a Hàng thu hồi không vào khu cách ly — vi phạm BR-019 (`L2-RET-01`) `[XÁC NHẬN RUNTIME — phát hiện MỚI 07/08/2026]`
File: `OrderService.cs:2680-2691` (`ConfirmPickupAsync`)

Phát hiện sau khi sửa xong 4 lỗi test-authoring trong `L2AfterSalesReturnTests.cs` (xem box đầu file) khiến toàn bộ luồng tạo yêu cầu đổi/trả → duyệt → lên lịch → xác nhận thu hồi giờ chạy trót lọt lần đầu tiên. `ConfirmPickupAsync` chỉ đổi `req.PickupStatus = PickupStatus.PickedUp` rồi `SaveChangesAsync()` — **không có bất kỳ dòng nào cộng `Inventory.QuarantineQuantity`**. Grep `QuarantineQuantity` toàn bộ `OrderService.cs` ra 0 kết quả ghi (chỉ có 1 chỗ khởi tạo = 0 trong `InventoryService.cs`).

**Việc cần làm:** trong `ConfirmPickupAsync`, sau khi đổi `PickupStatus`, cộng `Inventory.QuarantineQuantity` theo đúng số lượng + sản phẩm trong `req.ReturnItems` (tương tự cách `GoodsIssueService` cộng/trừ tồn có ghi `StockTransaction` — xem mục 1.2 để tiện làm chung 1 đợt vì cùng là thiếu ghi nhận biến động tồn kho).

### 1.4a GH-08 — Giỏ hàng hết hạn giữ giá 24h vẫn checkout được (`L1-ORD-09`, `L2-FUL-03`) `[XÁC NHẬN RUNTIME]`
File: `CartService.cs:52-70` (tự refresh giá thay vì chặn), `OrderService.cs` (`PlaceOrderAsync`/`GetCheckoutSummaryAsync` không check `PriceLockedAt`)

Đã được ghi nhận là **hoãn có chủ đích** trong `FIX_PLAN_2026-08-03.md` (xung đột với hành vi auto-refresh giá cố ý của `CartService`). Chạy thật (07/08/2026) xác nhận vẫn còn nguyên: checkout với giỏ `PriceLockedAt` quá 24h vẫn trả 200 và tạo đơn thành công. **Đề xuất**: đưa lại vào họp quyết định — nếu giữ hành vi auto-refresh của `CartService` thì đóng case (cập nhật lại kỳ vọng test); nếu SRS bắt buộc phải chặn, cần chọn 1 trong 2 hành vi, không thể vừa auto-refresh vừa yêu cầu chặn.

### 1.4 `L1-SJOB-11` — Cảnh báo tồn thấp sai ở đúng ngưỡng (off-by-one)
File: `VietTien.API/Services/ScheduledJobs/LowStockAlertJob.cs:45,84`

`if (inv.AvailableQuantity > inv.ReorderThreshold) continue;` → khi bằng đúng ngưỡng vẫn bị cảnh báo (SRS yêu cầu **chỉ** cảnh báo khi nhỏ hơn ngưỡng). Lặp lại y hệt ở dòng 84 cho vật tư (`SafetyThreshold`).

**Việc cần làm:** đổi điều kiện `continue` thành `>=` ở cả 2 chỗ (hoặc điều kiện cảnh báo thành `<` thay vì `<=` ngầm định hiện tại) — sửa 2 dòng, rủi ro thấp, nên làm sớm vì đơn giản.

---

## Phase 2 — 🟡 P2: Frontend — cần chạy ở repo `SEP490_fe`

> Đường dẫn thực tế của FE (không phải path trong report): `SEP490_fe/frontend` (Vite + React + Vitest + MSW).

| Case | File | Việc cần làm |
|---|---|---|
| `L1-FES-02` | `src/services/authService.js:4-27` (`fetchWithToken`) | Khi 401: gọi `refreshToken()` (đã có sẵn hàm, chỉ chưa được gọi ở đây) → nếu thành công, retry lại request gốc đúng 1 lần với token mới; chỉ redirect `/login` nếu refresh cũng thất bại. |
| `L1-FCMP-07` | `src/components/PhoneVerificationModal.jsx:29-52` | Thêm regex validate SĐT (10 số, bắt đầu bằng 0) trước khi gọi API; hiện chỉ check rỗng. |
| `L1-FCMP-06` | `src/pages/Checkout.jsx` (màn QR SePay, dòng 878-960) | Thêm countdown 15 phút thật cho màn QR (biến `countdown` hiện tại là bộ đếm 5s auto-redirect SAU khi thanh toán xong, khác mục đích) + trạng thái hết hạn (ẩn/disable QR, hướng dẫn liên hệ Sales). |
| `L1-FEC-08` | `src/context/CartContext.jsx:36-53` (`mergeGuestCartIntoServer`) | Đã giữ đúng `guestCart` khi lỗi (không mất hàng) nhưng chưa gọi `setError(...)` — chỉ cần thêm 1 dòng set error state để hiển thị thông báo cho khách. |
| `L1-FCMP-05` | `src/pages/Cart.jsx:323-433` | Đã hiện khối "Yêu cầu báo giá đặc biệt" đúng, nhưng nút "Đặt Hàng & Xem Hóa Đơn" (426-433) vẫn hiện song song — cần ẩn nút này khi `subtotal >= 100tr && !hasNegotiatedPrices`. |
| `L1-FCMP-04` | Chưa xác định file — cần audit lại `Checkout`/`Cart` component | Suy luận theo GH-08 (BE chưa chặn giá hết hạn 24h) nên banner cảnh báo phía FE nhiều khả năng cũng chưa có — **cần audit riêng, agent chưa xác minh trực tiếp case này**. |

---

## Phase 3 — 🔵 P3: Cần quyết định sản phẩm / không cần sửa code

### 3.1 OTP hardcode thay vì đọc SystemConfig (`L1-AUTH-20/28/29/30/31`)
`AuthService.cs` không inject `ISystemConfigService` — mọi ngưỡng OTP (hạn 5 phút, cooldown 60s, tối đa 5 lần/30 phút, 10 lần/ngày) là `const` cứng trong code, **hành vi runtime đúng** với giá trị mặc định trong SystemConfig nhưng **admin đổi config sẽ không có tác dụng gì**. Ngoài ra OTP điện thoại (`AUTH-20`) hoàn toàn thiếu giới hạn theo ngày (chỉ có 30 phút).

→ **Cần họp quyết định**: có bắt buộc phải đọc runtime từ `SystemConfig` không, hay giữ nguyên const (đơn giản hơn, ít rủi ro hơn)? Nếu bắt buộc: thêm `ISystemConfigService` vào `AuthService`, đọc `OTP_EXPIRE_MINUTES`/`OTP_RESEND_SECONDS`/`OTP_MAX_ATTEMPTS`, và thêm field `PhoneOtpSendCountDaily`/`PhoneOtpDayWindowStart` vào `User.cs` (1 migration) để hoàn thiện `AUTH-20`.

### 3.2 `L1-JOB-03` / `L2-SJOB-11` — Không có retry generic trong `JobRunService`
**Đây là quyết định có chủ đích, đã ghi nhận trong `FIX_PLAN_2026-08-03.md`**: comment sẵn có trong `JobRunServiceTests.cs` nêu rõ retry là trách nhiệm riêng của từng job cụ thể; thêm retry generic ở tầng `JobRunService` sẽ phá giả định idempotency của ~10 job khác đang chạy. → **Không tự sửa** — nếu SRS bắt buộc retry chung, cần thiết kế lại theo hướng mỗi job tự quyết có nên retry hay không (ví dụ thêm interface `IRetryableJob` opt-in), không sửa `RunTrackedAsync` chung cho tất cả.

`L2-SJOB-11` phụ thuộc cùng cơ chế này nên nhiều khả năng vẫn Fail khi chạy lại — không phải regression mới, cùng gốc với `JOB-03`.

### 3.3 Case đã xác nhận KHÔNG cần sửa (có test L3 thật rồi)
`L1-ORD-47`, `L1-ORD-53`, `L1-QUO-09`, `L1-AUD-07`, `L1-MKT-03`, `L1-DASH-05` — tất cả role-gate ở tầng Controller (`[Authorize(Roles=...)]`), đã có test tích hợp thật trong `VietTien.IntegrationTests/RoleGateTests.cs` xác nhận 403 đúng cho vai trò sai. Không cần hành động thêm — chỉ cần đảm bảo suite này được chạy trong CI (cần Docker + SQL Server).

### 3.4 Case đã fix schema — ĐÃ chạy lại thật, kết quả cập nhật 07/08/2026 `[XÁC NHẬN RUNTIME]`

Schema đã đủ bảng/cột (`MarketingPosts`, `GoodsReceipt.ImageProofUrl`, `GoodsIssue` đủ cột, `ReturnExchangeRequests` + 2 bảng con) — đã chạy lại `dotnet test VietTien.IntegrationTests` thật với Docker. Kết quả tách làm 2 nhóm rõ ràng:

**(a) Lỗi test, KHÔNG PHẢI bug ứng dụng — sửa test trước khi đánh giá lại:**
- `L2-RET-01/02/03/04`: `SeedStockAsync` (`VietTien.IntegrationTests/L2AfterSalesReturnTests.cs:38`) lấy `CustomerProfile` **đầu tiên trong DB** thay vì đúng profile của client `customer` vừa tạo trong test → đơn được seed thuộc về khách khác → `L1-ORD-51` (IDOR, đã fix đúng) chặn hợp lệ, lộ ra lỗi setup cũ vốn trước đây "pass may mắn" vì chưa có check quyền sở hữu. **Việc cần làm**: sửa `SeedStockAsync` để dùng đúng `CustomerProfile` của biến `customer`.
- `L2-PUR-04`: test gửi `ResolutionType` không thuộc enum hợp lệ (`AcceptExcess/ReturnExcess/RequestSupplemental/CloseShort`) ở bước resolve-discrepancy → 400 trước khi chạm được vào phần logic thật sự cần test (chặn close PO khi còn discrepancy). **Việc cần làm**: sửa test dùng đúng giá trị enum, chạy lại để biết business rule có đúng hay không.
- `L2-SJOB-10/11`: lỗi FK `FK_MarketingPosts_Users_CreatedByUserId` — test seed `MarketingPost` tham chiếu `UserId` chưa insert vào bảng `Users` (thứ tự seed sai). **Việc cần làm**: sửa thứ tự seed trong test.

**(b) Cần rà lại, chưa rõ là bug hay do test lỗi thời:**
- `L2-FLOW-02`: chặn bởi thông báo "Đơn hàng có lịch hẹn giao vào [ngày], không thể xác nhận giao trước ngày hẹn" — có vẻ là 1 business rule MỚI (lịch giao hàng theo SRS v2) mà kịch bản test COD 1 bước hoàn tất trong ngày chưa cập nhật theo. Cần xác nhận với đội nghiệp vụ đây có phải rule đúng không, rồi sửa test cho khớp ngày hẹn.

**(c) Bug ứng dụng thật, đã xác nhận — xem Phase 0/1:**
`L2-PAY-09` (0.1), `L2-PAY-10` (0.3), `L2-FUL-03` (mục dưới, GH-08), `L2-FUL-06` (1.1), `L2-TRF-01`/`L2-TRF-04` (1.2), `L2-PUR-05` (1.3), `L2-FLOW-06` (0.4), `L2-RET-06` (1.1, nặng hơn đánh giá ban đầu), `L2-RET-07` (0.2).

---

## Phát hiện thêm ngoài phạm vi 2 báo cáo (bonus finding từ `dotnet test` thật)

`VietTien.Tests.Services.StockTransferServiceTests.L1_ST_04_Dispatch_NonDraft_Rejected` đang **FAIL** trong lần chạy thật (không nằm trong danh sách 72 case của 2 báo cáo). Nguyên nhân: `StockTransferService.cs:212` đã đổi thông báo lỗi thành `"...trạng thái Nháp hoặc Đã xếp xe."` (mở rộng thêm trạng thái hợp lệ) nhưng test vẫn assert message cũ `"...trạng thái Nháp."` — nhiều khả năng chỉ là **test cũ chưa cập nhật theo business rule mới**, không phải bug thật. Sửa nhanh: cập nhật `WithMessage(...)` trong test cho khớp message mới, sau khi xác nhận với đội nghiệp vụ rằng "Đã xếp xe" được phép dispatch là chủ đích.

---

## Thứ tự đề xuất triển khai

1. **Phase 0** (0.1–0.4) — tiền/bảo mật, có thể gộp 0.1+0.2 vào **1 migration** (thêm `RowVersion` cho `Order`). Ưu tiên tuyệt đối.
2. **Phase 1** (1.1–1.4) — 1.1 và 1.4 sửa nhanh (sweep + 2 dòng), nên làm ngay sau Phase 0. 1.2 và 1.3 cần thiết kế nhỏ.
3. **Phase 2** — chuyển cho người phụ trách FE, làm song song với Phase 0/1 (không phụ thuộc).
4. **Phase 3** — họp quyết định (3.1, 3.2) trước khi động vào code; 3.3/3.4 chỉ cần chạy lại test để đóng case.
5. Sau mỗi phase: chạy `dotnet test VietTien.sln` (cần bật Docker cho phần `VietTien.IntegrationTests`) và cập nhật cột **Fix Status** trong 2 file xlsx.
